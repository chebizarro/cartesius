define(function(require) {
    // Load the dependencies
    var Boiler = require('Boiler');

// Preferences for the Layer component
// View open or closed
// Baselayer
// Visible Layers
// Open Folders

    var PrefModel = function (moduleContext) {
		var PanelOpen = true;
		
		var BaseLayer;
		
		var VisibleLayers;
		
		var OpenFolders;
		
        //this.checked = ko.observableArray([{id: 1}, {id: 2}]);
    };
    return PrefModel;
});
